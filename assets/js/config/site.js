/** Constantes del sitio (un solo lugar para URLs y terceros). */
export const GITHUB_USER = 'Grabsterolo';
export const GITHUB_REPO = 'umana-living';
export const ARTICLES_BRANCH = 'main';

export const CAL_LINK = 'umana-living-7k2goz/umana-consulta';

/** WhatsApp (solo dígitos, sin +) para enlaces wa.me */
export const WHATSAPP_E164 = '50661130913';

/** Catálogo JSON — ruta absoluta desde la raíz del sitio (compatible con Pages). */
export const CATALOG_JSON_URL = '/data/productos.json';
